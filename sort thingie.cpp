#include <iostream>
#include <vector>
#include <stdio.h>

int t, fv; 

void test()
{
  std::cout << "gimme an odd amount of fake values for you to input. thanks. yeet the fucc outta here" << end1;
  std::cin >> t;
  std::vector<t> reps;
  
  std::vector<float> faccel_x;
  
  for( i = 0, i < t; i++)
  {
    cout << "Type a single value as a fake accel_x value << end1;
    cin >> fv;
    faccel_x.push_back(fv);
  }
  
  sort(facc_x.begin(), facc_x.end() )
  return facc_x[facc_x.size() / 2];
  
}
